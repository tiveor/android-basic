<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.possiblelabs.layoutshowcase">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:theme="@style/AppTheme">



        <activity android:name=".MainActivity" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".relative.RelativeLayoutActivity"></activity>
        <activity android:name=".linear.LinearLayoutActivity"></activity>
        <activity android:name=".grid.GridLayoutActivity"></activity>
        <activity android:name=".tab.TabLayoutActivity"></activity>
        <activity android:name=".table.TableLayoutActivity"></activity>

        <activity android:name=".common.ImageViewActivity"></activity>

    </application>

</manifest>
